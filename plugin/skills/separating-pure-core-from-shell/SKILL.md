---
name: separating-pure-core-from-shell
description: Use when business logic cannot be tested without I/O, when deciding where a database or HTTP call belongs, or when structuring a service or application.
---

# Separating Pure Core From Shell

## Overview

A functional application has two parts. The **core** is pure: it computes
decisions from values, always returning the same output for the same
input, touching nothing outside itself. The **shell** is impure: it
reads, writes, waits, and talks to the world, and contains no business
rules.

Every property that makes functional code worth writing comes from this
split. A pure core is testable without infrastructure, safe to run
concurrently, easy to reason about, and cheap to reuse. The shell stays
small, boring, and mostly generated by the shape of the core.

## When to use

- Structuring a new service, application, or module
- A test needs a database to check a business rule
- Deciding where a service call goes inside a workflow
- Domain code imports a framework, driver, or transport library
- A rule and an effect are tangled in one function

Not for: choosing how to supply a specific dependency, which is
[parameterizing-dependencies](../parameterizing-dependencies/SKILL.md).

## The layers

```text
+-------------------------------------------+
| shell: transport, storage, clock, queues  |
|  +-------------------------------------+  |
|  | workflows: pipelines of pure steps   |  |
|  |  +-------------------------------+   |  |
|  |  | domain: types and rules        |   |  |
|  |  +-------------------------------+   |  |
|  +-------------------------------------+  |
+-------------------------------------------+
```

Dependencies point inward only. The domain knows nothing about workflows;
workflows know nothing about the shell. The shell knows everything, and
is the only layer allowed to be impure.

## Core rules

1. **All I/O at the edges.** Read what is needed, run the pure decision,
   write what it produced.
2. **The core returns decisions, not actions.** Events, instructions,
   updated values. The shell performs.
3. **Nothing inward-facing imports outward.** No driver, framework, or
   transport type appears in domain or workflow code.
4. **Time, randomness and identifiers are parameters.** They are effects,
   however innocent they look.
5. **The shell holds no rules.** If a decision is being made in the
   shell, it belongs in the core.
6. **Keep the shell thin and dull.** Its job is wiring, translation, and
   sequencing; nothing that needs a test to explain.
7. **The composition root wires the two together**, in one place.

## Pattern

Tangled:

```text
processRefund : RefundRequest -> AsyncResult<Unit, Error>
processRefund req =
  let booking = await Db.load req.bookingId              -- effect
  let days = daysBetween (Clock.now ()) booking.placed -- effect
  if days > 60 then Error TooOld                     -- rule
  else
    let amount = min req.amount booking.total          -- rule
    await Db.insertRefund booking.id amount            -- effect
    await Email.send booking.customer "refunded"       -- effect
    Ok ()
```

The two rules cannot be tested without a database, a clock, and a mail
server, and they are invisible among the plumbing.

Separated:

```text
-- core: pure, total, no imports
type RefundDecision =
  | Approve of { amount: Money, notify: EmailAddress }
  | Reject of RefundError

decideRefund : Instant -> Booking -> RefundRequest -> RefundDecision

-- shell: no rules, only sequencing
processRefund pool mailer now req =
  loadBooking pool req.bookingId
    |> map (\booking -> decideRefund now booking req)
    |> bind (performDecision pool mailer)
```

`decideRefund` is tested with three plain values and no setup. The shell
is four lines that nobody needs to reason about.

## What goes where

| Belongs in the core          | Belongs in the shell       |
| ---------------------------- | -------------------------- |
| Domain types and invariants  | HTTP, queues, sockets      |
| Validation and parsing rules | Database access            |
| Calculations and pricing     | Clock, random, identifiers |
| State transitions            | Retries and timeouts       |
| Deciding which events happen | Delivering those events    |
| Choosing what to store       | Performing the write       |
| Error classification         | Logging and metrics        |

More cases in
[what-belongs-where.md](references/what-belongs-where.md).

## Testing consequence

The split is what makes tests cheap, and the test suite's shape is a
reliable diagnostic:

- Core tests: plain values in, assertions on values out. No framework, no
  setup, fast enough to run thousands.
- Shell tests: a small number of integration tests proving the wiring,
  the mapping, and the transactions work.

If a business rule needs a database to test, the split has failed
somewhere, and the test difficulty is telling you where. See
[testing-functional-code](../testing-functional-code/SKILL.md).

## Red flags

- A domain module importing a driver, client, or framework
- A rule expressed inside a database query
- `now()` or `uuid()` called anywhere inside the core
- A workflow that sends an email itself
- Business logic in a controller, a resolver, or a job runner
- Tests for pricing that start a container
- A shell function containing a conditional about business meaning

## Common mistakes

- **Calling it done because there is a folder called domain.** The test
  is whether the code is pure, not where the file lives.
- **Pushing I/O out but keeping rules in the shell.** A shell that
  decides is the same problem with more files.
- **Making the core asynchronous throughout.** If the core needs `Async`
  everywhere, effects have leaked in.
- **A shell so thin it is a lie.** Transactions, retries, and ordering
  are real work; they belong in the shell and deserve care there.
- **Over-abstracting the boundary.** A function type per capability is
  enough. A plugin system is not.

## Related skills

- [parameterizing-dependencies](../parameterizing-dependencies/SKILL.md)
- [designing-workflow-pipelines](../designing-workflow-pipelines/SKILL.md)
- [crossing-io-boundaries](../crossing-io-boundaries/SKILL.md)
- [managing-state-immutably](../managing-state-immutably/SKILL.md)

## Further reading

- [architecture-shapes.md](references/architecture-shapes.md) relates the
  split to onion, hexagonal, and layered architectures, and shows the
  shell's real structure.
- [what-belongs-where.md](references/what-belongs-where.md) is a decision
  procedure for the ambiguous cases.
